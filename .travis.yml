language: python
python:
  - "3.5"
sudo: required

notifications:
  email:
    recipients:
      sbadillo@gmail.com
    on_success: never # default: change
    on_failure: always # default: always

branches:
  only:
  - sources

install:
- pip install -r requirements.txt
   
script:

- make publish github
